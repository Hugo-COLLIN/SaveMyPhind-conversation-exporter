{
  "categories": [
    {
      "title": "## What's Changed"
    }
  ],
  "template": {
    "release": {
      "changes": "* {{capitalize title}} by @{{author.username}} in {{url}}\n"
    },
    "change": "* {{capitalize title}} by @{{author.username}} in {{url}}",
    "group": "{{capitalize title}}\n{{#each changes}}{{{this}}}\n{{/each}}",
    "mainTemplate": "{{#each groups}}{{{this}}}\n{{/each}}**Full Changelog**: {{compareUrl}}"
  }
}
