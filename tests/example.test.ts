// import { describe, expect, test } from '@jest/globals';

describe('Example Test Suite', () => {
  test('should pass', () => {
    expect(true).toBe(true);
  });

  // test('chrome API mock works', () => {
  //   expect(chrome.runtime.getManifest().version).toBe('3.7.2');
  // });
});
