{
  "name": "save-my-phind",
  "version": "2.9.6",
  "license": "RMD-C v1.0 License",
  "scripts": {
    "--- Node scripts": "---",
    "syncVersion": "node manage/syncVersions.js",
    "licenses": "node manage/listLicenses.js",
    "convert": "node manage/convertManifest.js",
    "zip": "node manage/zip.js",
    "licenseList": "npm run syncVersion && npm run licenses",
    "--- Browser-specific scripts": "-----",
    "chrome": "npm run convert chrome && npm run build",
    "firefox": "npm run convert firefox && npm run build",
    "prodChrome": "npm run licenseList && npm run chrome && node manage/prodChanges.js && npm run zip",
    "prodFirefox": "npm run licenseList && npm run firefox && node manage/prodChanges.js && npm run zip",
    "--- Build scripts": "---",
    "build": "node manage/initBuild.js && esbuild src/scripts/background/background.js --bundle --outdir=dist && esbuild src/scripts/content/tab.js --bundle --outdir=dist && esbuild src/scripts/pages/pages.js --bundle --outdir=dist/pages",
    "build:l": "npm run licenseList && npm run build",
    "build:w": "nodemon --exec npm run build --watch",
    "watchAll": "nodemon --exec npm run all --watch",
    "--- Prod scripts": "---",
    "preprod": "npm run licenseList",
    "prod": "npm run licenseList && npm run firefox && node manage/prodChanges.js && npm run zip && npm run chrome && node manage/prodChanges.js && npm run zip && node manage/devChanges.js",
    "--- Test scripts": "---",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.8",
    "@babel/core": "^7.24.9",
    "@babel/plugin-proposal-decorators": "^7.24.7",
    "@babel/preset-env": "^7.23.7",
    "@types/chrome": "^0.0.266",
    "@types/jest": "^29.5.11",
    "adm-zip": "^0.5.10",
    "babel-jest": "^29.7.0",
    "chrome-remote-interface": "^0.33.0",
    "esbuild": "^0.18.11",
    "fs-extra": "^11.1.1",
    "gcp-refresh-token": "^0.9.2",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jest-fetch-mock": "^3.0.3",
    "license-checker": "^25.0.1",
    "nodemon": "^2.0.22",
    "selenium-webdriver": "^4.15.0"
  },
  "dependencies": {
    "@shoelace-style/shoelace": "^2.15.1",
    "dompurify": "^3.0.4",
    "js-yaml": "^4.1.0",
    "lit": "^3.1.4",
    "showdown": "^2.1.0",
    "turndown": "^7.1.2"
  }
}
